<% if message.user.system? %>
  <%= render 'messages/system_message', message: message %>
<% else %>
  <%= render 'messages/user_message',
             message: message,
             top: local_assigns[:top], bottom: local_assigns[:bottom].nil? ? true : bottom %>
<% end %>
